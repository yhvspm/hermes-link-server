# Protocol fixtures

These are public, non-secret, versioned wire examples. App and Cloud may keep
an identical copy for offline CI, but must verify its checksum against the
manifest during cross-repository release review. A fixture is not an internal
model and must not contain content, credentials, push tokens or private keys.
